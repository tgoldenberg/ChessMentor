.mentor-show-wrapper{data:{id: '#{current_user.id}'}}
  .top-wrapper
    .photo-wrapper
      .round-photo-wrapper
        = image_tag @user.avatar.url(:thumb), width: '104', class: 'thumb-profile'
    .greeting-wrapper
      %h2 #{@user.name}
      %p #{@user.city}
    .mentor-info-wrapper
      %th
        %p USCF ranking: #{@user.official_ranking}
      %th
        %p User ranking: #{@user.user_ranking}
      %th
        %p City: #{@user.city}
      %th
        %p Country: #{@user.country}
      %th
        %p #{number_to_currency(@user.price)} / class
  - if @conversation.messages.count <= 1
    .button-wrapper
      = link_to 'Message', "#", remote: true, class: 'button', id: 'message-button'
      = link_to 'Request Instruction', '#', class: 'button small-font'

    .form-wrapper{style: 'margin-top: 130px;'}
  - else
    .button-wrapper
      = link_to 'Request Instruction', '#', class: 'button small-font', style: 'font-size: 1.2em; padding: 19px 15px; left: 101px; top: 188px; position: absolute; letter-spacing: 1.2px;'

    .form-wrapper{style: 'margin-top: 150px;'}
      = render 'hidden_form'

:javascript
  $(function() {
    $('#message-button').click(function() {
      $('.form-wrapper').html("#{escape_javascript(render 'form')} ");
    });
  });
